{{backgrounds/gradient-background}}

{{#slide.content}}

  <div class="self-center p-16 w-full text-6xl">
    {{code-snippet
      name="inline.js"
      source="export default class extends Component {
  @tracked name = 'Sophie';
  @tracked age = 1;

  @tracked get greeting() {
    return `👋 ${this.name}, you're ${this.age}`;
  }
}"
    }}
  </div>

{{/slide.content}}
