{{stay-awake}}

{{#slide.content class="bg-yellow"}}
  <div class="self-center p-6 text-yellow text-center">
    <h2 class="font-hershey">Beethoven's 5th</h2>
  </div>

  <div class="self-center p-6 text-blue text-center">
    {{#if symphonyInstrument}}
      <h4 class='text-center font-hershey'>You are playing the</h4>
      <h1 class='text-center font-hershey'>{{symphonyInstrument}}</h1>
    {{else}}
      <h4 class='text-center'>...</h4>
    {{/if}}
  </div>
{{/slide.content}}