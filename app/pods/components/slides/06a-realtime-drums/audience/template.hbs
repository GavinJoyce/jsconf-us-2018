{{#slide.content}}
  <div class="self-center p-6 text-pink text-center">
    <h2 class="font-hershey">Realtime Drums</h2>
  </div>

  <div class="border-2 border-dashed border-blue text-xl {{if playingSound 'bg-blue'}} text-yellow p-4 w-4/5 m-auto text-center">
    <h1 class="font-hershey">
      {{#if playingSound}}
        {{playingSound}}
      {{else}}
        &nbsp;
      {{/if}}
    </h1>
  </div>
{{/slide.content}}