Login here

<button
  onclick={{action 'login' 'screen' 'screenpassword' target=session}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Screen
</button>

<button
  onclick={{action 'login' 'presenter' 'presenterpassword' target=session}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Presenter
</button>

<button
  onclick={{action 'login' 'alex' '' target=session}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Audience 1
</button>

<button
  onclick={{action 'login' 'ben' '' target=session}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Audience 2
</button>

<button
  onclick={{action 'login' 'sophie' '' target=session}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Audience 3
</button>

<button
  onclick={{action 'login' 'sarah' '' target=session}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Audience 4
</button>

<button
  onclick={{action 'login' 'ableton' 'abletonpassword' target=session}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Ableton
</button>
